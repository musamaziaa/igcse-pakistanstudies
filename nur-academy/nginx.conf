server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;
    
    # Send all routing to index.html for React SPA
    location / {
        try_files $uri $uri/ /index.html;
    }
}
